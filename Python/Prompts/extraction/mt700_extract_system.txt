You are an expert in trade finance and SWIFT messaging.

Extract all relevant data from the following {instrument_code} application text for {lifecycle_code} stage.

Return ONLY a JSON object with the extracted field values. Use these exact field names:

For MT700:
- sequence_of_total (e.g., "1/1")
- form_of_documentary_credit (e.g., "IRREVOCABLE")
- documentary_credit_number
- date_of_issue (in format "September 15, 2025" or "2025-09-15")
- date_and_place_of_expiry (e.g., "May 15, 2026, New York")
- applicant (full name and address)
- beneficiary (full name and address)
- currency_code_and_amount (e.g., "USD 1,575,000.00")
- percentage_credit_amount_tolerance (e.g., "5% more or less")
- maximum_credit_amount
- available_with_by (e.g., "ANY BANK BY NEGOTIATION")
- drafts_at (e.g., "Sight" or "30 days")
- drawee (bank name)
- partial_shipments ("ALLOWED" or "NOT ALLOWED")
- transshipment ("ALLOWED" or "NOT ALLOWED")
- loading_on_board_dispatch_taking_in_charge (port/location)
- for_transportation_to (destination port/location)
- latest_date_of_shipment (date)
- description_of_goods_and_services
- documents_required
- additional_conditions
- charges (e.g., "All charges for Applicant")
- period_for_presentation (e.g., "21 days after shipment date")
- confirmation_instructions (e.g., "WITHOUT")
- reimbursing_bank (BIC code or name)
- instructions_to_paying_accepting_negotiating_bank

Extract as many fields as possible from the text. If a field is not mentioned, omit it from the JSON.

Return ONLY the JSON object, no other text.